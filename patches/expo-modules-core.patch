diff --git a/android/build.gradle b/android/build.gradle
index 33c5d1417930aa545bc4069ad8961eb290a145ef..2e94ed630a425fe2e99f74600883480d31aa7e43 100644
--- a/android/build.gradle
+++ b/android/build.gradle
@@ -123,6 +123,7 @@ android {
 
   externalNativeBuild {
     cmake {
+      buildStagingDirectory new File(rootProject.rootDir, ".cxx/expo-modules-core")
       path "CMakeLists.txt"
     }
   }
@@ -266,7 +267,7 @@ if (shouldTurnWarningsIntoErrors) {
 // Generates minimal stub PCH files from an empty header so the IDE's C++ engine
 // doesn't fail during sync. Near-instant unlike the full ExpoHeader.pch.
 // The actual build regenerates proper PCH files via ninja.
-def cxxDir = project.file(".cxx")
+def cxxDir = new File(rootProject.rootDir, ".cxx/expo-modules-core")
 def generateStubPCHTask = tasks.register("generateStubPCH") {
   dependsOn("configureCMakeDebug")
 
